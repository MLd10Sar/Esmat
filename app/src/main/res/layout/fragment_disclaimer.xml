<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layoutDirection="rtl"
    tools:context=".ui.setup.DisclaimerFragment">

    <TextView
        android:id="@+id/tvDisclaimerHeading"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="24dp"
        android:layout_marginTop="32dp"
        android:layout_marginEnd="24dp"
        android:text="@string/disclaimer_heading"
        android:gravity="center"
        android:textAppearance="?attr/textAppearanceHeadline5"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:id="@+id/scrollView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        app:layout_constraintBottom_toTopOf="@+id/cbAgree"
        app:layout_constraintEnd_toEndOf="@id/tvDisclaimerHeading"
        app:layout_constraintStart_toStartOf="@id/tvDisclaimerHeading"
        app:layout_constraintTop_toBottomOf="@id/tvDisclaimerHeading">

        <TextView
            android:id="@+id/tvDisclaimerText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/user_responsibility_disclaimer"
            android:lineSpacingMultiplier="1.3"
            android:textAppearance="?attr/textAppearanceBody1" />

    </ScrollView>

    <CheckBox
        android:id="@+id/cbAgree"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="8dp"
        android:text="@string/disclaimer_i_agree"
        app:layout_constraintBottom_toTopOf="@+id/btnContinue"
        app:layout_constraintEnd_toEndOf="@id/tvDisclaimerHeading"
        app:layout_constraintStart_toStartOf="@id/tvDisclaimerHeading" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnContinue"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:enabled="false"
        android:text="@string/button_continue"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@id/tvDisclaimerHeading"
        app:layout_constraintStart_toStartOf="@id/tvDisclaimerHeading" />

</androidx.constraintlayout.widget.ConstraintLayout>